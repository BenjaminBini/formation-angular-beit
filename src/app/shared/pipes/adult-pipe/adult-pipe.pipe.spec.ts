import { AdultPipePipe } from './adult-pipe.pipe';

describe('AdultPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AdultPipePipe();
    expect(pipe).toBeTruthy();
  });
});
