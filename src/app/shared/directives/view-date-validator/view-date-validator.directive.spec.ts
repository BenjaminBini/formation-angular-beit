import { ViewDateValidatorDirective } from './view-date-validator.directive';

describe('ViewDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
